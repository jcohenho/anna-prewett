<p id="notice"><%= notice %></p>


<section>

  <h1>ANNA PREWETT</h1>
<% @posts.each do |post| %>
  <article>
    <%= link_to image_tag(post.image.url(:medium)), post_path(post) %>
    <aside><h3><%= post.title %></h3></aside>
  </article>
<% end %>
</section>
